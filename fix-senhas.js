const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function fix() { const hash = await bcrypt.hash('123456', 10); await prisma.usuario.updateMany({ data: { senha: hash } }); console.log('Todas as senhas foram redefinidas temporariamente para: 123456'); } fix().then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1) });
